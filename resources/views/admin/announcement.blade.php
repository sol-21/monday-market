<div>
    {{ $announcement->message }}
</div>
{{-- <form action="{{ route('unsubscribe', ['email' => $subscriber->email]) }}" method="POST">
    @csrf
    @method('DELETE')
    <button type="submit">Unsubscribe</button>
</form --}}